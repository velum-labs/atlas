"""alma-ports — Domain-scoped storage protocols (ports) for dependency inversion.

These protocols define the boundaries between the application/domain
layer and the infrastructure layer. Modules should depend on these
protocols rather than concrete storage implementations.
"""

__version__ = "0.1.0"
