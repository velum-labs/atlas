[project]
name = "alma-ports"
version = "0.1.0"
description = "Protocol interfaces for Alma Atlas — zero-dependency port definitions"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/alma_ports"]
